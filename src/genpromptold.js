/* const generatePrompt = async () => {
    var response = await ollama.generate({
      //Hard coded as this is just a demonstration app 
      model: "deepseek-r1:8b", // Change this to your preferred model
      prompt: input,
      stream: false,
} ) 

  // console.log(typeof(response.response))
  var answer = response.response;
  console.log(answer)
  // var regex = new RegExp("<think>.*<.think>","gis");
  // var textOut = answer.replace(regex," ")
  // This code is only relevant if using DeepSeek-r!
  // console.log(answer.match(regex).toString());
  //setOutput(textOut.toString())
  setOutput(answer);
  }; */
